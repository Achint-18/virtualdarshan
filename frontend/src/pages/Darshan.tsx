import {useState} from 'react';import MandalaBackdrop from '@/components/MandalaBackdrop';import FullscreenDarshan from '@/components/FullscreenDarshan';import SevaTeaser from '@/components/SevaTeaser';import PetalShower from '@/components/PetalShower';import {DARSHANS} from '@/lib/darshan-images';import type {GanpatiDarshan} from '@/lib/types';export default function Darshan(){const [selected,setSelected]=useState<GanpatiDarshan|null>(null);const [petals,setPetals]=useState(false);return <div className="relative min-h-screen overflow-hidden"><MandalaBackdrop/><div className="container py-12"><div className="text-center"><p className="text-sm tracking-widest text-amber-300">वर्चुअल दर्शन</p><h1 className="font-heading text-4xl text-amber-100 md:text-5xl">श्री गणपति बप्पा के शुभ दर्शन</h1><p className="mx-auto mt-3 max-w-xl text-orange-100/65">मन शांत करें, कुछ पल बप्पा के सामने बिताएँ।</p></div><div className="mt-10 grid auto-rows-[220px] grid-cols-2 gap-4 md:grid-cols-4">{DARSHANS.map((d,i)=><button key={d.id} onClick={()=>setSelected(d)} className={`group relative overflow-hidden rounded-3xl border border-amber-500/20 ${i===0?'col-span-2 row-span-2':'row-span-1'}`}><img src={d.image} className="absolute inset-0 h-full w-full object-cover transition duration-500 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-black/85 via-transparent to-black/10"/><div className="absolute bottom-4 left-4 text-left"><p className="font-heading text-xl text-amber-100">{d.title}</p><p className="text-xs text-orange-100/65">{d.subtitle}</p></div></button>)}</div><button className="btn btn-outline mt-6" onClick={()=>setPetals(v=>!v)}>{petals?'🌼 पुष्प वर्षा बंद':'🌼 पुष्प वर्षा'}</button></div>{petals&&<PetalShower/>}<SevaTeaser source="darshan"/><FullscreenDarshan darshan={selected} onClose={()=>setSelected(null)}/></div>}
