import {useQuery} from '@tanstack/react-query';import {apiGet} from '@/lib/api';import type {Counters} from '@/lib/types';export default function CountersStrip(){const {data}=useQuery({queryKey:['counters'],queryFn:()=>apiGet<Counters>('/counters'),refetchInterval:30000});return <div className="container grid grid-cols-2 gap-3 py-5"><div className="glass rounded-2xl p-4 text-center"><div className="text-2xl font-bold text-amber-300">{data?.darshan_count?.toLocaleString('en-IN')??'12,483'}</div><div className="text-xs text-orange-100/55">भक्तों के दर्शन · डेमो</div></div><div className="glass rounded-2xl p-4 text-center"><div className="text-2xl font-bold text-amber-300">₹{(data?.seva_today_total??0).toLocaleString('en-IN')}</div><div className="text-xs text-orange-100/55">आज की सेवा · डेमो</div></div></div>}
