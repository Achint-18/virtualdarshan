export default function PetalShower({count=18}:{count?:number}){return <div className="pointer-events-none fixed inset-0 z-40 overflow-hidden">{Array.from({length:count},(_,i)=><span key={i} className="absolute text-2xl" style={{left:`${(i*47)%100}%`,top:`${(i*19)%20}%`,animation:`fall ${3+(i%4)}s linear ${i*.12}s infinite`}}>🌼</span>)}<style>{'@keyframes fall{to{transform:translateY(110vh) rotate(360deg)}}'}</style></div>}
