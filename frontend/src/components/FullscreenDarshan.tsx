import {useEffect} from 'react';import {useNavigate} from 'react-router-dom';import {X} from 'lucide-react';import type {GanpatiDarshan} from '@/lib/types';import DiyaFlame from './DiyaFlame';import PetalShower from './PetalShower';export default function FullscreenDarshan({darshan,onClose}:{darshan:GanpatiDarshan|null;onClose:()=>void}){const nav=useNavigate();useEffect(()=>{if(!darshan)return;const h=(e:KeyboardEvent)=>e.key==='Escape'&&onClose();document.body.style.overflow='hidden';return()=>{document.body.style.overflow='';window.removeEventListener('keydown',h)}},[darshan,onClose]);if(!darshan)return null;return <div className="fixed inset-0 z-[60] bg-black"><img src={darshan.image} className="h-full w-full object-contain"/><div className="absolute inset-0 bg-gradient-to-t from-black via-transparent to-black/50"/><PetalShower count={10}/><button onClick={onClose} className="absolute right-4 top-4 rounded-full bg-black/50 p-3 text-white"><X/></button><div className="absolute bottom-8 left-1/2 w-full -translate-x-1/2 px-5 text-center"><DiyaFlame size="sm"/><h2 className="font-heading text-3xl text-amber-100">{darshan.title}</h2><p className="text-orange-100/75">{darshan.blessing}</p><button className="btn btn-primary mt-4" onClick={()=>{onClose();nav('/aarti')}}>🪔 आरती करें</button></div></div>}
